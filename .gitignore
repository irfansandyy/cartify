# VS Code workspace (allow common project files)
.vscode/*

# === Java / Maven ===
# Build outputs
/target/
# Common Java artifacts
*.class
*.jar
*.war
*.ear
# JVM crash logs
hs_err_pid*
replay_pid*

# Maven release/versions plugin files
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
buildNumber.properties
.mvn/timing.properties

# Keep Maven wrapper scripts; don't ignore .mvn/wrapper
!/.mvn/wrapper/maven-wrapper.jar

# === Node / Frontend ===
node_modules/
# Build outputs
/dist/
/build/
.cache/
# Logs
npm-debug.log*

# Env files (keep examples)
.env
.env.*
!.env.example

# === Java WebApp deploy dirs inside source (avoid committing runtime content) ===
# These should be produced at build/package time
/src/main/webapp/WEB-INF/classes/
/src/main/webapp/WEB-INF/lib/
